'use strict';

const users = () => {
    const model = {
        username:``,
        password:``,
        email:``,    
        notelp:``,
        name:``,  
    }
    return model;
}

module.exports = {
    users: users
}
